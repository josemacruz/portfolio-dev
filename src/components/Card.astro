---
import Link from "@/components/Link.astro";
interface Props {
  id: string;
  backgroundColor: string;
  className: string;
  urlType?: string;
  url?: string;
}

const { id, backgroundColor, className, url, urlType } = Astro.props;
---

<section id={id} class={`card ${className}`}>
  <div>
    {url && <Link width={40} url={url} className={urlType ?? ""} />}
  </div>
  <main><slot /></main>
</section>

<style define:vars={{ color: backgroundColor }}>
  section {
    display: flex;
    justify-content: flex-end;
    padding: 0.5rem;
    position: relative;
  }

  div {
    position: absolute;
  }

  main {
    padding: 1rem;
    width: 100%;
    text-align: center;
  }
  .card {
    box-shadow: 0px 0px 15px 1px rgba(0, 0, 0, 0.33);
    border-radius: 20px;
    background-color: var(--color);
  }

  .about {
    grid-column: span 2;
    grid-row: span 2;
  }

  .experience_1 {
    grid-column: span 2;
    grid-row: span 2;
  }

  .experience_2 {
    grid-column: span 2;
    grid-row: span 2;
  }

  .blog {
    grid-column: span 2;
  }
</style>
